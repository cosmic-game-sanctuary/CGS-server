.env
.env.*
!.env.example
*.pem
*.key
secrets/

# Dependencies
node_modules/

# Build output
dist/
build/
*.tsbuildinfo

# Logs
*.log
npm-debug.log*
logs/

# Runtime / local state
.DS_Store
coverage/
.cache/
tmp/
uploads/
# Uploaded builds, kept so they can be served. The CID on the game row is the
# real record; this is a cache of it. See services/games/buildStore.ts.
storage/

# Editor
.vscode/
.idea/

# Internal working rules — kept local, not published.
CLAUDE.md

# Drizzle local artifacts (migrations ARE committed; meta journal is too)
drizzle/.snapshot-tmp/
